<div class="row single-column">
  <div class="columns">
    
    <div class="page-content page-fullscreen">
      {% if settings.pages-breadcrumb-show %}
        {% include 'snippet-breadcrumbs' %}
      {% endif %}
      <div class="rte-content colored-links">
        {{ page.content }}
      </div>
    </div>

  </div>
</div>