<div class="main-wrapper">

  <ul id="lens-options">

    <li>sun lens</li>

    <li>polarized</li>

    <li class="active">optic</li>

  </ul>

</div>

<div id="lens-box-desktop" class="lens-box main-wrapper">
  
  {% for link in linklists.all-collections.links %}

    {% assign collection = link.object %}

    {% assign collectionname  = collection.title %}
    
    {% if collectionname != 'caps' and collectionname != 'outerwear'  %}

    <div class="goggles" style="left:0px;">

    <!-- goggles -->
    <h2 class="text-centered text-goggle">{{ collection.title }}</h2>
    
    {% for product in collection.products %}

      {% assign product_lens_options  = product.title | split: ' - ' %}

      <div style="display:none;background-image: url({{ product.featured_image.src | product_img_url: "original" }})" class="goggle-thumb {{ product_lens_options[2] | replace: ' ', '-' }}">

            <span class="overlay-white">
              
              <div class="goggles-block centered-table">

                <div>

                  <p id="cad-currency" class="text-centered">{{product.title}}</p>
                  
                  <p class="text-centered">{{product.price | money}}</p>

                  <a href="{{product.url}}" class="black-btn view-goggles">view</a>
                  
                </div>

              </div>

            </span>

      </div>

      {% comment %}<a style="display:none;" class="goggle-thumb {{ product_lens_options[2] | replace: ' ', '-' }}" href="{{product.url}}">
        
        <div class="goggles-block">
          
          <img src="{{ product.featured_image.src | product_img_url: "original" }}" alt="">

          <span class="product-title-hover">{{ product.title }}</span>

        </div>

      </a>{% endcomment %}

    {% endfor %}

    </div>

    {% endif %}

  {% endfor %}

</div>

<div id="lens-box-mobile"  class="lens-box" style="display:none;">
  
  {% for link in linklists.all-collections.links %}
  {% assign collection = link.object %}

    {% assign collectionname  = collection.title %}
    
    <!-- caps -->
    {% if collectionname == 'caps' %}
    <div class="caps-container">
      
      <h2 class="text-centered text-caps">&nbsp;</h2>
      
      {% for product in collection.products %}

        {% assign product_lens_options  = product.title | split: ' - ' %}

        <a style="display:none;" class="caps-thumb goggle-thumb {{ product_lens_options[2] | replace: ' ', '-' }}" href="{{product.url}}">
          
          <div class="goggles-block">
            
            <img src="{{ product.featured_image.src | product_img_url: "original" }}" alt="">

            <span class="product-title-hover">{{ product.title }}</span>
            
          </div>

        </a>
      
      {% endfor %}
    </div>
    {% else %}
  
    <!-- goggles -->
    <h2 class="text-centered text-goggle">{{ collection.title }}</h2>

    <span style="width:100%;display:inline-block;font-size:9px;" class="text-centered text-goggle">swipe for other colors</span>
    <!-- sun lens -->
    <div class="goggles swiper-container-sun-lens sun-lens" style="left:0px;">
      <div class="swiper-wrapper sun-lens">
      {% for product in collection.products %}

      {% assign product_lens_options  = product.title | split: ' - ' %}
      
        {% if product_lens_options[2] == 'sun lens' %}
        <div class="swiper-slide">
        <a style="display:none;" class="goggle-thumb {{ product_lens_options[2] | replace: ' ', '-' }}" href="{{product.url}}">
          
          <div class="goggles-block">
            
            <img src="{{ product.featured_image.src | product_img_url: "original" }}" alt="">

            {% if collection.title == 'caps' %}
              
            {% endif %}

            <span class="product-title-hover">{{ product.title }}</span>

          </div>

        </a>
        </div>
        {% else %}
        {% endif %}
      {% endfor %}
      </div>
    </div>
    

    <!-- polarized -->
    <div class="goggles swiper-container-polarized polarized" style="left:0px;">
      <div class="swiper-wrapper polarized" style="display:none;">
    
      {% for product in collection.products %}

      {% assign product_lens_options  = product.title | split: ' - ' %}
      
      {% if product_lens_options[2] == 'polarized' %}
      <div class="swiper-slide">
      <a style="display:none;" class="goggle-thumb {{ product_lens_options[2] | replace: ' ', '-' }}" href="{{product.url}}">
        
        <div class="goggles-block">
          
          <img src="{{ product.featured_image.src | product_img_url: "original" }}" alt="">

          {% if collection.title == 'caps' %}
            
          {% endif %}

          <span class="product-title-hover">{{ product.title }}</span>

        </div>

      </a>
      </div>
      {% else %}
      {% endif %}
      {% endfor %}
      </div>
    </div>


    <!-- optic -->
    <div class="goggles swiper-container-polarized optic" style="left:0px;">
      <div class="swiper-wrapper optic" style="display:none;">
    
      {% for product in collection.products %}

      {% assign product_lens_options  = product.title | split: ' - ' %}
      
      {% if product_lens_options[2] == 'optic' %}
      <div class="swiper-slide">
      <a style="display:none;" class="goggle-thumb {{ product_lens_options[2] | replace: ' ', '-' }}" href="{{product.url}}">
        
        <div class="goggles-block">
          
          <img src="{{ product.featured_image.src | product_img_url: "original" }}" alt="">

          {% if collection.title == 'caps' %}
            
          {% endif %}

          <span class="product-title-hover">{{ product.title }}</span>

        </div>

      </a>
      </div>
      {% else %}
      {% endif %}
      {% endfor %}
      </div>
    </div>

    {% endif %}

  {% endfor %}

</div>