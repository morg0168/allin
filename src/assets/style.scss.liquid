// developer : jonathan dion
// contact : jonathandionalary@gmail.com

// mixins

/*============================================================================
  Dependency-free breakpoint mixin
    - http://blog.grayghostvisuals.com/sass/sass-media-query-mixin/
==============================================================================*/
$min: min-width;
$max: max-width;
@mixin at-query ($constraint, $viewport1, $viewport2:null) {
  @if $constraint == $min {
    @media screen and ($min: $viewport1) {
      @content;
    }
  } @else if $constraint == $max {
    @media screen and ($max: $viewport1) {
      @content;
    }
  } @else {
    @media screen and ($min: $viewport1) and ($max: $viewport2) {
      @content;
    }
  }
}

$viewportIncrement: 1px;
$siteWidth: 1050px;
$small: 590px;
$medium: 768px;
$large: 769px;
$xlarge: $siteWidth + $viewportIncrement;

$postSmall: $small + $viewportIncrement;
$preMedium: $medium - $viewportIncrement;
$preLarge: $large - $viewportIncrement;

/*================ The following are dependencies of csswizardry grid ================*/
$breakpoints: (
  'small' '(max-width: #{$small})',
  'medium' '(min-width: #{$postSmall}) and (max-width: #{$medium})',
  'medium-down' '(max-width: #{$medium})',
  'large' '(min-width: #{$large})'
);
$breakpoint-has-widths: ('small', 'medium', 'medium-down', 'large');
$breakpoint-has-push:  ('medium', 'medium-down', 'large');
$breakpoint-has-pull:  ('medium', 'medium-down', 'large');

@mixin clearfix() {
  &:after {
    content: '';
    display: table;
    clear: both;
  }
}

@mixin font-smoothing () {
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

@mixin responsive-typo ( $min-size : 12, $max-size : 21, $min-width : 420, $max-width : 1280 ) {
  font-size: calc( #{$min-size + px} + #{$max-size - $min-size} * ( (100vw - #{$min-width + px}) / #{$max-width - $min-width}));
  @media screen and (max-width: $min-width + px ) {
    font-size :  $min-size + px;
  }
  @media screen and (min-width: $max-width + px) {
    font-size : $max-size + px;
  }
}

@mixin prefix($property, $value) {
  -webkit-#{$property}: #{$value};
  -moz-#{$property}: #{$value};
  -ms-#{$property}: #{$value};
  -o-#{$property}: #{$value};
  #{$property}: #{$value};
}

// animations
@-webkit-keyframes animLeftSlidshow {
    0% {
        -webkit-transform: translate(0, -50%);
        -moz-transform: translate(0, -50%);
        -ms-transform: translate(0, -50%);
        -o-transform: translate(0, -50%);
        transform: translate(0, -50%)
    }
    100% {
        -webkit-transform: translate(-50%, -50%);
        -moz-transform: translate(-50%, -50%);
        -ms-transform: translate(-50%, -50%);
        -o-transform: translate(-50%, -50%);
        transform: translate(-50%, -50%)
    }
}
@-moz-keyframes animLeftSlidshow {
    0% {
        -webkit-transform: translate(0, -50%);
        -moz-transform: translate(0, -50%);
        -ms-transform: translate(0, -50%);
        -o-transform: translate(0, -50%);
        transform: translate(0, -50%)
    }
    100% {
        -webkit-transform: translate(-50%, -50%);
        -moz-transform: translate(-50%, -50%);
        -ms-transform: translate(-50%, -50%);
        -o-transform: translate(-50%, -50%);
        transform: translate(-50%, -50%)
    }
}
@-ms-keyframes animLeftSlidshow {
    0% {
        -webkit-transform: translate(0, -50%);
        -moz-transform: translate(0, -50%);
        -ms-transform: translate(0, -50%);
        -o-transform: translate(0, -50%);
        transform: translate(0, -50%)
    }
    100% {
        -webkit-transform: translate(-50%, -50%);
        -moz-transform: translate(-50%, -50%);
        -ms-transform: translate(-50%, -50%);
        -o-transform: translate(-50%, -50%);
        transform: translate(-50%, -50%)
    }
}
@-o-keyframes animLeftSlidshow {
    0% {
        -webkit-transform: translate(0, -50%);
        -moz-transform: translate(0, -50%);
        -ms-transform: translate(0, -50%);
        -o-transform: translate(0, -50%);
        transform: translate(0, -50%)
    }
    100% {
        -webkit-transform: translate(-50%, -50%);
        -moz-transform: translate(-50%, -50%);
        -ms-transform: translate(-50%, -50%);
        -o-transform: translate(-50%, -50%);
        transform: translate(-50%, -50%)
    }
}
@keyframes animLeftSlidshow {
    0% {
        -webkit-transform: translate(0, -50%);
        -moz-transform: translate(0, -50%);
        -ms-transform: translate(0, -50%);
        -o-transform: translate(0, -50%);
        transform: translate(0, -50%)
    }
    100% {
        -webkit-transform: translate(-50%, -50%);
        -moz-transform: translate(-50%, -50%);
        -ms-transform: translate(-50%, -50%);
        -o-transform: translate(-50%, -50%);
        transform: translate(-50%, -50%)
    }
}

// base

/**
 * Typography
 */
@font-face {
  font-family: 'Maison Neue Medium';
  src: url('MaisonNeue-Medium.eot'); /* IE9 Compat Modes */
  src: url('MaisonNeue-Medium.eot?#iefix') format('embedded-opentype'), /* IE6-IE8 */
       url('MaisonNeue-Medium.woff') format('woff'), /* Modern Browsers */
       url('MaisonNeue-Medium.ttf')  format('truetype'), /* Safari, Android, iOS */
       url('MaisonNeue-Medium.svg#751d4e146592611362c6dd26f9bc3016') format('svg'); /* Legacy iOS */

  font-style:   normal;
  font-weight:  400;
}

* {
  font-family: 'Maison Neue Medium', sans-serif;
}



// components
.c-slide-show {
  position: relative;
  overflow: hidden;

  .bg {
    background-size: contain;
    background-repeat: no-repeat;
    background-position: center center;
    max-height: 1000px;
    width: 100%;
    min-height: 350px;
    position: relative;
    @include at-query($min, $medium) {
      height: 90vh;
      margin-top: -3vh;
    }
  }

  // img {
  //   width: 100%;
  //   display: block;
  // }

  .overflow-hidden {
    overflow: hidden;
  }

  .relative {
    position: relative;
  }

  .slideshow-titre {
    position: absolute;
    display: block;
    z-index: 20;
    top: 50%;
    left: 0;
    display: inline-block;
    transform: translateY(-50%);
    margin-top: -2.5%;
    white-space: nowrap;
  }

  .slideshow-titre.back {
    left: 0;
  }
  .slideshow-img-ctn {
      position: relative;
      z-index: 10;
  }


  .slideshow-slide  {
    padding: 0 20px;
    @include at-query($min, $large) {
      padding: 0 50px;
    }
    @include at-query($min, $xlarge) {
      padding: 0 100px;
    }
  }

  .slideshow-titre {
    animation: animLeftSlidshow 16s linear infinite;
    left: -20px;
    font-size: 60px;
    font-weight: bold;

    @include at-query($min, $small) {
      font-size: 160px;
    }
  }

}

// Out of stock style

$red : #FF0012;

.container-sold-out {
  display: flex;
  align-items: center;
  text-align: center;
  height: 100%
}

.container-sold-out p.container-sold-out__text {
	margin-bottom: 5em;
}

.container-sold-out a.container-sold-out__btn{
	color: white !important;
}

.overlay-sold-out:hover {
  background: rgba(0, 0, 0, 0.5);
}

.u-red-bg {
  background-color: $red !important;
}

.u-red-txt {
	color: $red !important;
}